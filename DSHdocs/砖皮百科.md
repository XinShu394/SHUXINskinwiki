# 砖皮百科

- 会话: session-af3d0b42-fb37-44cf-9eda-cd8b9ad61999
- 更新时间: 2026/8/31 13:34:53
- 状态: 已完成
- 任务进度: 已完成 0/8 项

## 任务待办
- [ ] **进行中** 搭建新UI预览目录结构与数据文件(data.js 模拟真实数据)
- [ ] 实现画风设计(绿黑底+青绿强调+芥末黄小标+Loop字阶)
- [ ] 实现P0进入页(双图标+悬停+点击过渡)
- [ ] 实现P1选枪页(封面墙,无侧栏)
- [ ] 实现P2枪种图鉴页(薄侧栏+皮肤网格)+P3详情
- [ ] 实现P4上传选择页(三功能一亮两暗)
- [ ] 实现P5公开只读待审 / P6投稿四步 / P7密钥审批
- [ ] 本地静态预览验证(不依赖后端,图片失败用线稿占位回退)

## 涉及 / 修改的文件
- `{"command": "pdfinfo '新UI资源\\页面流程修改.pdf'", "description": "Show PDF metadata"}`
- `{"command": "pdftoppm -png -r 150 '新UI资源\\页面流程修改.pdf' '新UI资源\\flow'; Write-Host 'done'; Get-ChildItem '新UI资源' -Filter 'flow*.png' | Select-Object Name, Length", "description": "Render PDF to PNG at 150 DPI"}`
- `{"command": "$port=8137; foreach($f in 'index.html','styles.css','app.js','data.js'){ try { $r = Invoke-WebRequest -Uri \"http://127.0.0.1:$port/$f\" -UseBasicParsing -TimeoutSec 8; \"$f -> $($r.StatusCode) ($($r.Content.Length)b)\" } catch { \"$f ERR $($_.Exception.Message)\" } }", "description": "Verify all preview assets serve"}`
- `{"command": "Get-Command node -ErrorAction SilentlyContinue | Select-Object Source; if(Get-Command node -ErrorAction SilentlyContinue){ node --check 'C:\\砖皮鉴赏\\砖皮百科\\新UI资源\\预览\\app.js'; node --check 'C:\\砖皮鉴赏\\砖皮百科\\新UI资源\\预览\\data.js'; Write-Host 'JS syntax OK' } else { Write-Host 'node not found' }", "description": "Check JS syntax with node"}`
- `{"file_path": "C:\\砖皮鉴赏\\砖皮百科\\新UI资源\\预览\\app.js", "old_string": "          <p class=\"lead\">参考图：<span style=\"color:var(--green)\">材质确认图</span> 与 <span style=\"color:var(--green)\">投稿推荐图</span>（本地 `site/guide/`）。</p>", "new_string": "          <p class=\"lead\">参考图：<span style=\"color:var(--green)\">材质确认图</span> 与 <span style=\"color:var(--green)\">投稿推荐图</span>（本地 site/guide/）。</p>"}`
- `{"command": "node --check 'C:\\砖皮鉴赏\\砖皮百科\\新UI资源\\预览\\app.js'; node --check 'C:\\砖皮鉴赏\\砖皮百科\\新UI资源\\预览\\data.js'; Write-Host 'JS OK'", "description": "Re-check JS syntax"}`
- `{"command": "python -m http.server 8137 --directory 'C:\\砖皮鉴赏\\砖皮百科\\新UI资源\\预览'", "description": "Serve new UI preview on port 8137", "run_in_background": true}`
- `{"command": "Start-Sleep -Seconds 2; foreach($f in 'index.html','styles.css','app.js','data.js'){ try { $r = Invoke-WebRequest -Uri \"http://127.0.0.1:8137/$f\" -UseBasicParsing -TimeoutSec 8; \"$f -> $($r.StatusCode) ($($r.Content.Length)b)\" } catch { \"$f ERR\" } }", "description": "Verify preview assets accessible"}`
- `{"command": "$chrome = @('C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe','C:\\Program Files (x86)\\Google\\Chrome\\Application\\chrome.exe','C:\\Program Files\\Microsoft\\Edge\\Application\\msedge.exe') | Where-Object { Test-Path $_ }; $chrome", "description": "Find a Chromium browser for headless screenshots"}`
- `{"command": "Remove-Item '新UI资源\\预览\\.chrome-tmp' -Recurse -Force -ErrorAction SilentlyContinue; Remove-Item '新UI资源\\预览\\.edge-tmp' -Recurse -Force -ErrorAction SilentlyContinue; Get-ChildItem '新UI资源\\预览' -Force | Select-Object Name", "description": "Remove leftover temp dirs and list final files"}`

---
*由 dsh-task-docs 自动记录；供多 Agent 同步与人工确认进度。*

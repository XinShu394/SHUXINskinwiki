# M7 接入与上线流程总结

更新时间：2026-06-11

## 本次目标

- 完成 `M7` 全量枪皮标准图生成（A/B/C/D）。
- 接入站点数据与封面入口，支持本地预览与线上发布。
- 完成逐图命名并沉淀命名过程文档。

## 已完成工作

1. 素材标准化
   - 对 `M7` 全部目录按统一规则生成标准图：
     - `A`：`Snipaste` 原图（不裁剪）
     - `B`：左15% / 上10% / 右6% / 下18%
     - `C/D`：仅上裁 2.2%
   - 结果：47 个目录、188 张标准图。
   - 清理：删除原始 `Snipaste_*.png` 与 `Delta Force Screenshot*.png`，仅保留标准图。

2. 构建规则扩展
   - 在 `scripts/validate_and_build.py` 新增 `material_color` 模式，支持 `M7` 这类目录解析（如 `UQ白粉005`）。
   - 在 `scripts/config/weapon_rules.json` 增加：
     - `weapon: "M7"`
     - `dir: "M7"`
     - `mode: "material_color"`

3. 站点接入
   - 更新 `site/data.js`：追加 47 条 `M7` 记录。
   - 更新 `site/meta.js`：写入 M7 展示名（47 条）。
   - 更新 `site/covers.js`：`M7` 入口启用（`enabled: true`）。

4. 本地测试环境
   - 新建 `本地测试/`：
     - `index.html`
     - `data.local.js`
     - `covers.local.js`
   - 本地测试版将 OSS 前缀改为 `../`，直接读取本地目录图片，便于“先验收后上传”。

5. 命名与文档沉淀
   - 按 B 图逐张命名，不在仓库保留总览 JPG（仅保留文本结果）。
   - 命名进度与结果：
     - `docs/M7命名进度.md`

## 本次发布顺序（已验证）

1. 本地验证 `本地测试/index.html`（确保名称、筛选、详情图正常）。
2. 上传 OSS：`M7/` 全目录（188 张标准图）。
3. 推送 GitHub 改动。
4. ECS `git pull` 同步站点代码。
5. 浏览器强刷验证线上展示。

## 注意事项

- 站点前端依赖数据结构严格一致：
  - `window.SKIN_DATA` 必须是数组
  - `window.WEAPON_COVERS` 必须是数组
- 本地预览不要直接用 `file://`，应通过 `启动.bat` 的本地服务访问。
- 已标记待复核项：
  - `M7-UQ1112-001` 的 B 图可能不是标准“市场查看图”，需后续人工确认图位顺序。


# 砖皮百科 · 项目结构

结论：这是一个「静态图鉴站点 + Flask 业务 API + OSS 图片存储」的 UGC 产品。代码在 GitHub，图片在 OSS，业务数据在 ECS 本地 SQLite。

线上入口：`skin.textagent.cn`（Nginx 托管 `site/`，`/api/` 反代到 Flask `:5200`）

---

## 总览

```
用户浏览器
    ├─ HTML/CSS/JS ──────────→ ECS Nginx（site/）
    ├─ 武器皮肤图片 ─────────→ 阿里云 OSS（skinwiki）
    └─ 评论 / 投稿 / 审核 ───→ Flask API :5200（server/app.py + SQLite）
                                    └─ 审核通过 → build_worker 跑 scripts/validate_and_build.py
```

本地开发：根目录双击 `启动.bat` → `scripts/dev_server.py` 在 `:8765` 提供静态站点；评论/投稿需另开 `server/app.py`（`:5200`）。

---

## 根目录一览

| 路径 | 类型 | 作用 |
|---|---|---|
| `site/` | 代码 | 前端静态站点（图鉴、投稿、审核、评论） |
| `server/` | 代码 | Flask API、构建 worker、备份、systemd / Nginx 配置 |
| `scripts/` | 代码 | 目录校验、站点数据构建、本地开发服务器、入库脚本 |
| `启动.bat` | 入口 | 本地一键启动静态预览（`:8765`） |
| `.gitignore` | 配置 | 排除武器图片、SQLite、venv、docs 等 |
| `部署架构说明.md` | 文档 | 线上部署与 OSS / DNS / 环境变量 |
| `结构.md` | 文档 | 本文件：仓库结构说明 |
| `皮肤内部UI修改.md` | 文档 | 空占位 |
| `ASVAL/` `AUG/` `K416/` `KC17/` `M4A1/` `M7/` `M250/` `MP7/` `QBZ95/` `SCARH/` `Vector/` `腾龙/` | 素材 | 各武器皮肤截图（gitignore，本地/OSS 持有） |
| `枪皮总封面/` | 素材 | 首页武器封面图（gitignore） |
| `投稿页面图/` | 素材 | 投稿引导原图（站点实际用 `site/guide/`） |
| `浓缩SOP/` | 本地资料 | 操作 SOP（gitignore 不提交） |
| `docs/` | 本地资料 | 工作记录（gitignore 不提交） |

`AKM` 已在 `weapon_rules.json` 与 `site/data/AKM.js` 中配置，根目录暂无对应图片文件夹（图片走 OSS）。

---

## `site/` · 前端

单页应用。生产入口 `index.html`，本地对照入口 `index.local.html`（加载 `local-paths.js`，把 OSS 图改成本地相对路径）。

### 页面与脚本

| 文件 | 职责 |
|---|---|
| `index.html` | 主页面骨架：首页、列表、详情、投稿/审核/补充图弹层 |
| `index.local.html` | 本地测试页 |
| `styles.css` | 全站样式 |
| `app.js` | 导航、筛选、列表/详情渲染、点赞、本地校验按钮 |
| `submit.js` | 投稿四步流：推荐 → 筛选 → 查重 → STS 直传 OSS |
| `review.js` | 审核通道：token 登录、待审列表、通过/拒绝 |
| `supplement.js` | 详情页「补充图」上传 |
| `comments.js` | 评论读写、点赞 |
| `local-paths.js` | 仅本地页：OSS URL → `../武器/目录/文件` |
| `README.md` | 站点维护说明 |

### 数据文件（构建脚本生成，勿手改为主）

| 文件 | 职责 |
|---|---|
| `data/{武器}.js` | 每把枪的皮肤数组（`window._SKIN_DATA_*`） |
| `data.js` | 把各武器数组合并为 `window.SKIN_DATA` |
| `meta.js` | `window.SKIN_META`：皮肤展示名等 |
| `covers.js` | `window.WEAPON_COVERS`：首页封面与是否启用 |

### 其他

| 路径 | 职责 |
|---|---|
| `guide/` | 投稿引导图（推荐构图、材质确认路径） |

`index.html` 脚本加载顺序：各 `data/*.js` → `data.js` → `meta.js` → `covers.js` → `comments.js` → `app.js` → `submit.js` → `review.js` → `supplement.js`。

---

## `server/` · 后端

主服务 `app.py`（Flask，监听 `127.0.0.1:5200`）。生产用 gunicorn（见 `start.sh` / `zpbk-api.service`）。

### 核心代码

| 文件 | 职责 |
|---|---|
| `app.py` | 全部 API：评论、点赞、投稿 STS、commit 校验、审核、补充图 |
| `build_worker.py` | 轮询 `build_jobs`，调用 `scripts/validate_and_build.py` |
| `backup_db.py` | 把 `comments.db` 快照上传到 OSS `backups/` |
| `notes_backfill.py` | 历史投稿心得回填 `approved_skin_id`（一次性运维） |
| `requirements.txt` | flask / flask-cors / gunicorn / Pillow / oss2 / 阿里云 STS SDK |
| `nginx.conf` | Nginx 配置模板（静态 + `/api` 反代） |
| `start.sh` | ECS 上创建 venv 并用 gunicorn 启动 API |

### systemd

| 文件 | 职责 |
|---|---|
| `zpbk-api.service` | API 开机自启 |
| `zpbk-build-worker.service` | 构建 worker 常驻 |
| `zpbk-backup.service` | 一次性备份任务 |
| `zpbk-backup.timer` | 每天 03:00 触发备份 |

### 运行时（不进 Git）

- `comments.db`：评论、投稿、点赞、构建队列
- `venv/`：Python 虚拟环境
- `uploads/pending/`：旧 local 上传目录（当前主路径是 OSS `pending/`）

### API 一览

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/ping` | 健康检查 |
| GET/POST | `/api/comments` | 读/写评论 |
| PUT | `/api/comments/<id>/like` | 评论点赞 |
| GET | `/api/skin-stats` | 皮肤点赞数 + 评论数 |
| GET | `/api/skin-notes` | 已审通过的投稿心得 |
| POST | `/api/skins/<id>/like` | 皮肤点赞/取消 |
| POST | `/api/submissions/create` | 创建投稿并签发 STS |
| POST | `/api/submissions/commit` | 校验 OSS 对象并进入待审 |
| GET | `/api/submissions/query` | 用票据查审核状态 |
| GET | `/api/submissions` | 待审列表（需管理员 token） |
| PUT | `/api/submissions/<id>/approve` | 通过：拷贝到正式目录 + 入构建队列 |
| PUT | `/api/submissions/<id>/reject` | 拒绝 |
| GET | `/api/uploads/<id>/<slot>` | 审核预览图（签名 URL） |
| GET | `/api/supplements` | 某皮肤已通过的补充图 |
| POST | `/api/submit` | 旧接口，OSS 模式下返回 410 |

---

## `scripts/` · 构建与工具

| 文件 | 职责 |
|---|---|
| `validate_and_build.py` | 核心：按规则解析皮肤目录，校验 A/B/C/D 图，写入 `site/data/*.js`、`data.js`、`meta.js`、`covers.js`。支持 `--weapon`、`--source local\|oss`、`--dry-run`、`--normalize-folders` |
| `dev_server.py` | 本地静态服务器 `:8765`，并提供 `/api/validate` 给前端「校验并更新」按钮 |
| `diag_submission.py` | 诊断投稿：模拟 folderCode → skin_id |
| `ingest_mp7.py` | 把 MP7 原始截图裁切/重命名为 `_A/_B/_C/_D` |
| `ingest_m250.py` | 同上，针对 M250 |
| `config/weapon_rules.json` | 武器 → 目录名、解析 mode（asval / k416 / akm / material_color / template_with_material / pure_template 等） |
| `config/meta_overrides.json` | 展示名覆盖（当前为空） |

常用命令：

```bash
python scripts/validate_and_build.py --weapon K416
python scripts/validate_and_build.py --source oss --weapon MP7
python scripts/dev_server.py
```

---

## 素材目录约定（gitignore）

每把武器一个文件夹。皮肤子目录名由品级/材质/颜色编码组成（或模板名），构建脚本据此生成 `skin_id`。

单条皮肤标准图位：

| 槽位 | 含义 |
|---|---|
| `_A` | 市场缩略图（封面） |
| `_B` | 市场详情大图 |
| `_C` | 室内持枪效果 |
| `_D` | 室外持枪效果 |
| `_S1` `_S2` `_S3` … | 玩家补充图（审核通过后写入） |

线上图片 URL：

```
https://skinwiki.oss-cn-guangzhou.aliyuncs.com/{武器名}/{皮肤文件夹}/{文件名}
```

待审对象前缀：`pending/{submission_id}/`（私有，7 天生命周期清理）。

---

## 主数据流

```
投稿
  submit.js
    → POST /api/submissions/create（签发 STS）
    → 浏览器直传 OSS pending/{id}/A|B|C|D|S*
    → POST /api/submissions/commit（Head + 魔数校验）
    → status = pending_review

审核
  review.js
    → PUT .../approve：copy 到正式武器目录，分配唯一 skin_id，enqueue build_jobs
    → PUT .../reject：只改状态，pending 交给 OSS 生命周期删除

发布
  build_worker.py
    → python scripts/validate_and_build.py --weapon <枪>
    → 更新 site/data/*.js、data.js、meta.js、covers.js
    → Nginx 已托管这些静态文件，刷新即可看到
```

---

## 本地 vs 生产

| | 本地 | 生产（ECS `/var/www/zpbk`） |
|---|---|---|
| 静态页 | `启动.bat` → `:8765` | Nginx `:80`，根路径打开 `site/index.html` |
| 图片 | 可走 OSS，或 `index.local.html` 走本地武器目录 | OSS 公共读 |
| API | 另开 `python server/app.py` `:5200` | gunicorn + `zpbk-api.service`，Nginx `/api/` 反代 |
| 构建 | 前端「校验并更新」或手动跑脚本 | `zpbk-build-worker` 消费队列 |
| 数据库 | `server/comments.db` | 同路径，每日备份到 OSS |

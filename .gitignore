# ── 武器图片文件夹（上传到 OSS，不进 GitHub）──────────────
ASVAL/
AUG/
K416/
KC17/
M4A1/
M250/
M7/
MP7/
QBZ95/
SCARH/
Vector/
腾龙/
枪皮总封面/

# ── 旧版本副本（可以直接删掉，占了 ~5.7GB）─────────────────
skin-wiki/
skinwiki/

# ── IDE / 系统文件 ────────────────────────────────────────
.cursor/
.DS_Store
Thumbs.db

# ── Python 运行时 ─────────────────────────────────────────
server/venv/
server/__pycache__/
server/*.pyc
__pycache__/

# ── 评论数据库（生产数据，不进代码仓库）────────────────────
server/comments.db

# ── 本地知识资产（仅本地保留，不提交）──────────────────────
docs/
浓缩SOP/
部署架构说明.md

# ── 构建脚本生成文件（从 OSS 实时重建，不进 git）─────────────
# ECS 由 build_worker 从 OSS 生成这些文件，不需要 git 追踪
# 本地开发若需数据，跑 validate_and_build.py --source oss 即可
site/data/
site/meta.js
site/covers.js
site/reports/
